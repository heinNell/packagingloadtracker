# Packaging Load Tracker - Docker Environment Variables

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=packagingloadtracker

# Server Configuration
JWT_SECRET=generate-a-secure-random-string-for-production
CORS_ORIGIN=http://localhost

# Optional: External database URL (overrides individual DB settings)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
